# 消息发送中心 (MessageCenter) 项目说明报告

## 一、项目概述

### 1.1 项目简介
消息发送中心是一个基于 SpringBoot 构建的分布式消息推送平台，支持多渠道消息发送（短信、邮件、公众号、小程序、企业微信、钉钉等），具备千万级消息处理能力。系统采用责任链模式、消息队列、线程池等技术，实现高可用、高性能的消息推送服务。

### 1.2 技术栈
- **核心框架**: SpringBoot 2.5.6, Mybatis Plus
- **数据库**: MySQL 5.7+
- **消息队列**: Kafka (支持可插拔设计，可切换 EventBus/RocketMQ/RabbitMQ)
- **缓存**: Redis
- **配置中心**: Apollo (支持弱依赖，可降级为本地配置)
- **日志系统**: GrayLog + ELK
- **监控系统**: Prometheus
- **定时任务**: XXL-Job
- **流处理**: Flink 1.14.3
- **API 文档**: Swagger 3.0.0
- **工具库**: Hutool, Guava, Fastjson, OkHttp
- **线程池**: Dynamic-TP (动态线程池)

### 1.3 Java 版本
- JDK 1.8

---

## 二、项目架构

### 2.1 模块结构

```
MessageCenter (父工程)
├── msg-common           # 公共模块：实体、常量、枚举、工具类
├── msg-handler          # 消息处理器：各渠道消息发送的实际处理逻辑
├── msg-service-api      # 服务接口层：定义服务接口，不提供实现
├── msg-service-api-impl # 服务实现层：责任链处理、业务逻辑实现
├── msg-support          # 业务支持层：DAO 操作、责任链编排执行
├── msg-web              # Web 暴露层：Controller、对外 REST API
├── msg-cron             # 定时任务模块：XXL-Job 任务处理
├── msg-stream           # 流处理模块：Flink 数据清洗
└── msg-client           # 客户端模块
```

### 2.2 端口配置
| 服务 | 端口 | 说明 |
|------|------|------|
| 项目主服务 | 8080 | SpringBoot 应用端口 |
| Zookeeper | 2181 | Kafka 依赖 |
| Kafka | 9092 | 消息队列 |
| Kafka-Manager | 9000 | Kafka 管理界面 |
| Redis | 6379 | 缓存服务 (密码：123456) |
| Apollo | 8080/8070 | 配置中心 (后台：http://127.0.0.1:8070, apollo/admin) |
| GrayLog | 9009 | 日志系统 |
| Prometheus | 3000 | 监控系统 |

---

## 三、核心功能

### 3.1 消息发送流程

#### 3.1.1 实时消息发送
1. **接口调用**: 调用 `/sms/sendSmsTest` 接口，传入手机号和消息模板 ID
2. **任务封装**: 构建 `TaskInfo` 消息任务模型，封装责任链上下文
3. **责任链执行**: 
   - 前置参数校验
   - 参数组装
   - 后置参数校验
   - 发送消息至 MQ
4. **MQ 推送**: 数据推送到 Kafka 主题
5. **消费者监听**: 
   - `MsgReceiver` 监听 Kafka 消息
   - 根据 `ChannelType × MessageType` 匹配对应线程池
   - 校验 `groupId` 一致性
6. **异步处理**: 
   - 从 `TaskPendingHolder` 获取指定线程池
   - 执行 `Task` 任务，调用 Handler 的 `handler()` 方法
7. **消息发送**: 实际渠道 Handler 执行发送并记录日志

#### 3.1.2 定时任务批量发送（千万级数据处理）
1. **任务触发**: XXL-Job 定时任务触发 `CronTaskHandler.execute()`
2. **异步处理**: `@Async` 注解启用动态线程池，避免阻塞
3. **文件读取**: 
   - 读取 CSV 人群文件（支持千万级数据）
   - 逐行读取，避免内存溢出
4. **缓冲队列**: 
   - 数据放入 `AbstractLazyPending` 队列
   - 设置数量阈值和时间阈值
5. **批量消费**: 
   - 单线程循环监控队列 (`initConsumePending`)
   - 达到阈值时触发 `doHandle()`
6. **批量发送**: 
   - `CrowdBatchTaskPending.doHandle()` 聚合相同参数消息
   - 调用 `SendService.batchSend()` 批量发送

**线程池设计**:
- **线程池 1**: `AsyncConfiguration` - 处理定时任务异步执行
- **线程池 2**: `CrowdBatchTaskPending.ExecutorService` - 消费队列数据并推送

**数据丢失应对**:
- 内存队列重启会丢失数据
- 通过统计下发人数、处理人数、到达人数进行核对
- 消息场景对数据一致性要求相对较低

---

### 3.2 责任链模式应用

#### 3.2.1 设计原理
责任链上有多个节点，每个节点都有机会处理请求，处理完成后可传递给下一节点或返回结果。类似 Spring 拦截器链、Servlet 过滤器链。

#### 3.2.2 消息发送责任链
```
PipelineConfig 配置责任链内容：
1. 参数校验 Handler
2. 参数组装 Handler  
3. 后置校验 Handler
4. MQ 发送 Handler
```

#### 3.2.3 代码结构
```java
// 抽象处理类
public abstract class AbstractLeaveHandler {
    protected AbstractLeaveHandler nextHandler; // 下一节点
    protected void handlerRequest(LeaveRequest request); // 处理方法
}

// 具体实现类
public class OneLeaveHandler extends AbstractLeaveHandler { ... }
public class TwoLeaveHandler extends AbstractLeaveHandler { ... }
```

---

### 3.3 多渠道消息支持

#### 3.3.1 支持的渠道类型
| Code | 渠道 | 说明 |
|------|------|------|
| 10 | IM | 即时通讯 |
| 20 | Push | 推送通知 |
| 30 | 短信 | 短信发送 |
| 40 | Email | 邮件发送 |
| 50 | 公众号 | 微信公众号 |
| 60 | 小程序 | 微信小程序 |
| 70 | 企业微信 | 企业微信消息 |
| 80 | 钉钉机器人 | 钉钉群机器人 |
| 90 | 钉钉工作通知 | 钉钉工作消息 |
| 100 | 企业微信机器人 | 企微群机器人 |
| 110 | 飞书机器人 | 飞书群机器人 |

#### 3.3.2 接入新渠道步骤
1. **引入依赖**: 在主 POM 和 `msg-support` 中引入相关 SDK 依赖
2. **扩展枚举**: 
   - `ChannelType` 添加新渠道枚举值
   - `IdType` 添加接收人类型枚举值
3. **实现 Handler**: 
   - 在 `msg-handler` 中创建新的 Handler 类
   - 继承 `BaseHandler` 实现 `handler()` 方法
   - 实现参数拼接、接口请求、结果解析逻辑

---

### 3.4 单机限流

#### 3.4.1 限流实现
使用 Google Guava 的 `RateLimiter` 实现单机限流，支持两种策略：
- **REQUEST_RATE_LIMIT**: 按请求次数限流
- **SEND_USER_NUM_RATE_LIMIT**: 按发送用户数限流

#### 3.4.2 配置方式
```java
// Handler 初始化时配置限流参数
public EmailHandler() {
    channelCode = ChannelType.EMAIL.getCode();
    Double rateInitValue = 3.0; // 默认 3 QPS
    flowControlParam = FlowControlParam.builder()
        .rateInitValue(rateInitValue)
        .rateLimitStrategy(RateLimitStrategy.REQUEST_RATE_LIMIT)
        .rateLimiter(RateLimiter.create(rateInitValue))
        .build();
}
```

#### 3.4.3 动态调整
通过 Apollo 配置中心动态调整限流值：
```properties
# Apollo 配置示例
key: flowControl
value: {"flow_control_40": 1}  # 渠道 40 限流 1 QPS
```

---

### 3.5 短信流量负载均衡

#### 3.5.1 自定义注解
```java
@SmsScriptHandler("TencentSmsScript")  // 标识短信服务商
public class TencentSmsScript extends BaseSmsScript { ... }
```

#### 3.5.2 负载均衡策略
基于权重随机算法：
1. 计算总权重值
2. 生成随机数确定区间
3. 优先使用主服务商，备份服务商作为降级

#### 3.5.3 Apollo 配置示例
```properties
key: msg_type_sms_config
value: [{"message_type_10":[
    {"weights":80,"scriptName":"TencentSmsScript"},
    {"weights":20,"scriptName":"YunPianSmsScript"}
]}]
```
- 80% 流量走腾讯云
- 20% 流量走云片
- 主服务商失败自动切换备份

---

### 3.6 MQ 可插拔设计

#### 3.6.1 设计理念
通过 `@ConditionalOnProperty` 注解实现 MQ 的无缝切换，支持：
- Kafka
- EventBus (单机模式)
- RocketMQ
- RabbitMQ

#### 3.6.2 配置生效条件
```java
@Component
@ConditionalOnProperty(name = "msg.mq.pipeline", havingValue = MessageQueuePipeline.KAFKA)
public class KafkaSendMqServiceImpl implements SendMqService { ... }
```

#### 3.6.3 消息 Tag 过滤 (Kafka)
**发送端**: 将 tag 写入 Kafka Header
```java
List<Header> headers = Arrays.asList(
    new RecordHeader(tagIdKey, tagId.getBytes())
);
kafkaTemplate.send(new ProducerRecord(topic, null, null, null, jsonValue, headers));
```

**消费端**: 配置过滤器
```java
factory.setRecordFilterStrategy(consumerRecord -> {
    // 检查 Header 中的 tagId，非自身 tag 返回 true(丢弃)
    return !isMatchTag(consumerRecord);
});
```

---

### 3.7 动态线程池

系统使用三种线程池：

| 线程池 | 配置类 | 类型 | 用途 |
|--------|--------|------|------|
| MQ 消费线程池 | `HandlerThreadPoolConfig` | 动态线程池 | 消费 Kafka 消息 |
| 定时任务文件处理 | `CronAsyncThreadPoolConfig.getXxlCronExecutor` | 动态线程池 | 处理 XXL-Job 任务 |
| 定时任务消息推送 | `CronAsyncThreadPoolConfig.getConsumePendingThreadPool` | 普通线程池 | 批量推送消息 |
| 单线程消费 | `SupportThreadPoolConfig` | 单线程池 | 循环消费队列 |

**动态线程池特性**:
- 支持 Apollo 配置动态调整核心参数
- 实时监控线程池状态
- 优雅关闭支持

---

## 四、数据库设计

### 4.1 核心表结构

#### 4.1.1 message_template (消息模板表)
存储消息模板配置信息，包括：
- 模板名称、审核状态、消息状态
- 定时任务配置 (Cron 表达式、人群文件路径)
- 消息类型 (ID 类型、发送渠道、模板类型、消息类型)
- 消息内容 (支持占位符 `{$var}`)
- 创建者、更新者、审核人、业务方信息

#### 4.1.2 channel_account (渠道账号表)
存储各渠道的账号配置：
- 账号名称、发送渠道
- 账号配置 (JSON 格式存储敏感信息)
- 创建/更新时间、删除标记

#### 4.1.3 sms_record (短信记录表)
记录短信发送详情：
- 模板 ID、手机号、供应商 ID/名称
- 短信内容、批次 ID、计费条数
- 回执内容、发送状态、发送日期

---

## 五、关键类说明

### 5.1 消息处理相关
| 类名 | 作用 |
|------|------|
| `BaseHandler` | 消息处理抽象基类，统一处理流程 |
| `SmsHandler` | 短信消息处理器，支持负载均衡 |
| `EmailHandler` | 邮件消息处理器 |
| `PushHandler` | Push 推送处理器 |
| `MiniProgramHandler` | 小程序消息处理器 |
| `EnterpriseWeChatRootHandler` | 企业微信根处理器 |

### 5.2 服务层相关
| 类名 | 作用 |
|------|------|
| `SendService` | 消息发送服务接口 |
| `SendServiceImpl` | 消息发送服务实现 |
| `RecallService` | 消息撤回服务 |
| `ConfigService` | 配置服务 (Apollo/本地降级) |

### 5.3 MQ 相关
| 类名 | 作用 |
|------|------|
| `SendMqService` | MQ 发送接口 |
| `KafkaSendMqServiceImpl` | Kafka 发送实现 |
| `EventBusSendMqServiceImpl` | EventBus 发送实现 |
| `MsgReceiver` | Kafka 消息监听器 |
| `ReceiverStart` | Kafka 监听启动配置 |

### 5.4 定时任务相关
| 类名 | 作用 |
|------|------|
| `CronTaskHandler` | 定时任务入口处理器 |
| `TaskHandler` | 任务处理接口 |
| `TaskHandlerImpl` | 任务处理实现 |
| `CrowdBatchTaskPending` | 批量人群任务挂起处理 |
| `AbstractLazyPending` | 惰性消费抽象类 |

### 5.5 限流相关
| 类名 | 作用 |
|------|------|
| `FlowControlService` | 限流服务接口 |
| `FlowControlServiceImpl` | 限流服务实现 |
| `FlowControlParam` | 限流参数配置类 |

---

## 六、部署与配置

### 6.1 环境依赖
1. **JDK 1.8**
2. **MySQL 5.7+**
3. **Redis 3.0+**
4. **Kafka 2.x+** (或使用 EventBus 单机模式)
5. **Zookeeper** (Kafka 依赖)
6. **Apollo** (可选，支持本地配置降级)
7. **XXL-Job-Admin** (定时任务调度中心)

### 6.2 配置文件
主要配置项位于 `application.yml` 或 Apollo 配置中心：
```yaml
msg:
  mq:
    pipeline: kafka  # 或 event_bus/rocketmq/rabbitmq
  business:
    topic:
      name: send_topic
    tagId:
      key: tagId
      value: default
```

### 6.3 启动步骤
1. 执行 `sql/msg_center.sql` 初始化数据库
2. 配置 `application.yml` 或 Apollo 配置
3. 启动依赖服务 (Redis/Kafka/Zookeeper/Apollo)
4. 运行 `mvn clean install` 编译项目
5. 启动主应用 `msg-web`

---

## 七、API 接口

### 7.1 消息发送接口
- **POST** `/sms/sendSmsTest` - 测试短信发送
- **POST** `/send` - 通用消息发送接口
- **POST** `/batchSend` - 批量消息发送接口

### 7.2 模板管理接口
- **GET** `/messageTemplate/list` - 查询模板列表
- **POST** `/messageTemplate/save` - 保存模板
- **PUT** `/messageTemplate/update` - 更新模板
- **DELETE** `/messageTemplate/delete/{id}` - 删除模板

### 7.3 渠道账号接口
- **GET** `/channelAccount/list` - 查询渠道账号列表
- **POST** `/channelAccount/save` - 保存渠道账号

### 7.4 数据接口
- **GET** `/data/timeLine` - 获取用户时间线数据
- **GET** `/data/material` - 获取素材数据

**Swagger 文档**: http://127.0.0.1/swagger-ui/

---

## 八、监控与日志

### 8.1 日志系统
- **GrayLog**: 集中式日志管理
- **Logback-Gelf**: 日志格式化为 GELF 协议
- **@LogRecord**: 注解打印业务日志

### 8.2 监控系统
- **Prometheus**: 指标采集与存储
- **Grafana**: 可视化展示 (端口 3000)
- **Dynamic-TP**: 动态线程池监控

### 8.3 链路追踪
- 通过 `AnchorInfo` 记录消息流转节点
- 记录发送成功/失败状态
- 支持业务 ID 追踪

---

## 九、性能优化

### 9.1 千万级消息处理
- **逐行读取 CSV**: 避免一次性加载导致 OOM
- **缓冲队列**: `LinkedBlockingQueue` 控制内存占用
- **批量发送**: 达到阈值后批量推送，减少 IO 次数
- **多线程消费**: 独立线程池处理队列数据

### 9.2 线程池优化
- **动态调整**: 根据负载动态调整线程数
- **拒绝策略**: `CallerRunsPolicy` 避免任务丢失
- **优雅关闭**: 等待任务完成后再关闭

### 9.3 MQ 优化
- **Tag 过滤**: 减少无效消息消费
- **多 Partition**: Kafka 多分区提升并发
- **多消费者组**: 按渠道/消息类型隔离

---

## 十、扩展建议

### 10.1 功能扩展
1. **分布式限流**: 集成 Sentinel 实现集群限流
2. **消息优先级**: 支持高优先级消息插队处理
3. **消息重试**: 失败消息自动重试机制
4. **死信队列**: 处理多次重试失败的消息

### 10.2 性能提升
1. **Redis 分布式锁**: 防止重复发送
2. **消息去重**: 基于 Redis Bloom Filter
3. **异步落库**: 发送日志异步写入数据库
4. **冷热分离**: 历史数据归档到冷存储

### 10.3 可靠性增强
1. **消息持久化**: 发送前持久化到数据库
2. **补偿机制**: 定时任务扫描未发送消息
3. **幂等性**: 基于唯一 ID 保证消息不重复
4. **多活部署**: 跨区域容灾部署

---

## 十一、总结

消息发送中心是一个功能完善、架构清晰的分布式消息推送平台，具备以下特点：

✅ **多渠道支持**: 覆盖主流消息推送渠道  
✅ **高并发处理**: 支持千万级消息批量发送  
✅ **灵活扩展**: 插件化设计，快速接入新渠道  
✅ **可靠稳定**: 限流、降级、监控等机制完善  
✅ **易维护**: 模块化设计，代码规范清晰  

项目采用了多种设计模式（责任链、工厂、策略等）和中间件技术，是学习企业级消息系统设计的优秀案例。

---

**报告生成时间**: 2024 年  
**项目版本**: 1.0-SNAPSHOT  
**技术负责人**: imwj
